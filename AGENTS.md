# Implementation policy

Before changing or adding implementation code, read `docs/specification.md`.

Treat `docs/specification.md` as the authoritative specification. Later amendments in that file override earlier notes. Do not infer missing requirements; keep them as TODOs until a specification decision is made.
